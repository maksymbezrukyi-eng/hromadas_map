# WORKLOG

## 2026-04-13

* Завершено v0.5 safe split фронтенду на окремі модулі CSS/JS.
* Виправлено bugfix `statusColor(...)` -> `statusMarkerColor(...)`.
* Виправлено bugfix safe `resetF()` when `f-kfw` is absent.
* Прибрано публічні згадки UNICEF з фронтенду.
* Виправлено баг, коли після sync межі громад поверталися до legacy KfW-кольорів.
* Виправлено runtime bug з повторним оголошенням `H` у split `index.html`.
* Стратегічно погоджено рух від selection tool до monitoring tool.
* Нова тимчасова таблиця проаналізована; пряма міграція ще не починалась.
